<!DOCTYPE html>
html
    head
        meta(charset="UTF-8")
        link(rel="stylesheet", href="css/main.css")
    body
        .feature-product
            div(class=['container', 'feature-container'])
                div(class=['feature-boxes', 'feature-boxes-horizontal'])
                    - var arrIcon = ["delivery", "sale", "quality"]
                    - var arrTitle = ["Free Delivery", "Sales & discounts", "Quality assurance"]
                    - for(var i=0; i<3; i++)
                        a(href!="#", class='feature-box')
                            .feature-box-icon
                                img(src="img/"+arrIcon[i]+".svg" alt="delivery")
                            .feature-box-info
                                h4.feature-box-title=""+arrTitle[i]
                                p.feature-box-description.
                                    Worldwide delivery on all. Authorit tively morph next-generation innov tion with extensive models.
